import SectionContainer from "@/components/SectionContainer";

const JobDetails = () => {
    return (
        <SectionContainer>
            <h1>Job Details</h1>
        </SectionContainer>
    );
};

export default JobDetails;
