import SectionContainer from "@/components/SectionContainer";

const Applications = () => {
    return (
        <SectionContainer>
            <h1>Applications</h1>
        </SectionContainer>
    );
};

export default Applications;
